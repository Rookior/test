"use strict";

var PI = 3.14;
var sum = function sum(x, y) {
    return x + y;
};
var _name$age = {
    name: "张三",
    age: 18

    // 通过执行npx babel src/index -o dist/index.js 将当前js编译为es5语法到dist文件夹

},
    name = _name$age.name,
    age = _name$age.age;
